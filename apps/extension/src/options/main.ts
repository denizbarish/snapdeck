/**
 * Options page shell. The pairing-token field, its validation and the bridge
 * status line arrive in task 9.
 *
 * Plain TypeScript against the DOM, no framework: the finished page is one text
 * field, two buttons and a status line, and a UI library would be the
 * extension's only runtime dependency.
 */
export {}
