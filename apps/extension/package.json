{
  "name": "@snapdeck/extension",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build && vite build --config vite.content.config.ts && node scripts/check-manifest.mjs",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@snapdeck/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "@vitest/browser": "3.2.7",
    "playwright": "1.63.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "3.2.7"
  }
}
