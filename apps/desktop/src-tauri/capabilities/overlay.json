{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "The overlay window is built hidden and shows itself once the frozen frame has painted, so the user never faces a transparent click-swallowing rectangle. Nothing else is needed: the frame's absolute path arrives as a query parameter, and reading the PNG uses the asset protocol scope declared in tauri.conf.json.",
  "windows": ["overlay-*"],
  "permissions": ["core:window:allow-show"]
}
